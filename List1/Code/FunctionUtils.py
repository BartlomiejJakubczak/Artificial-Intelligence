import struct


#class FunctionUtils:

    # def toBinary(self, coefficient):
    #     return ''.join('{:0>8b}'.format(c) for c in struct.pack('!f', coefficient))
    #
    # # returns number as an string integer code representation IEEE 754 32 bit float
    #
    # def toFloat(self, binary_number):
    #     integered = int(binary_number, 2)
    #     unpacked = struct.unpack('f', struct.pack('I', integered))[0]
    #     return unpacked

    # def setBinaryCoefficients(self, fun):
    #     fun.binary_coefficients = [self.toBinary(fun.a),
    #                                self.toBinary(fun.b),
    #                                self.toBinary(fun.c),
    #                                self.toBinary(fun.d),
    #                                self.toBinary(fun.e),
    #                                self.toBinary(fun.f)]


    # def plotFunction(self):

